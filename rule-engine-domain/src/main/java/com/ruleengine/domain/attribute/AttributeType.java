package com.ruleengine.domain.attribute;

/**
 * Enumeration of supported attribute data types.
 * Used to define the type of values that attributes can hold.
 * 
 * Module: rule-engine-domain
 * Layer: Domain
 */
public enum AttributeType {
    STRING,
    NUMBER,
    BOOLEAN,
    DATE,
    DATETIME,
    DECIMAL
}

