package com.ruleengine.persistence.entity;

/**
 * Enum for comparison operators in persistence layer.
 * Mirrors the domain ComparisonOperator enum.
 * 
 * Module: rule-engine-persistence
 * Layer: Persistence
 */
public enum ComparisonOperatorEntity {
    GT,
    GTE,
    LT,
    LTE,
    EQ,
    NE
}

